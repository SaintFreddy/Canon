# Canon

This repository currently contains the control-plane scaffold for the platform plan.

- Accepted planning and control-plane artifacts live under `docs/control-plane/`.
- Repo-scoped Factory execution assets live under `/.factory/`.
- The current status, phase plan, and carry-forward log live in `docs/control-plane/core/master-plan.md`.

There is no app/runtime implementation in this repo yet.